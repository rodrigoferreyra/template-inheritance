/**
 * CE.SDK utilities used by the catalog resolver (setSourceSet image helpers, colors).
 */

export {
  hexToRgba,
  replaceImageByName,
  setColorFillByName,
  exportSceneAsImage,
} from "./utils";
export type { ImageSource, ImageSourceInput } from "./utils";
export type { RgbaColor } from "./types";
